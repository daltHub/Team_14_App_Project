<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Display_meeting_avail"></activity>
        <activity android:name=".meeting_setter" />
        <activity android:name=".displayresult" />
        <activity android:name=".Voting_v2" />
        <activity
            android:name=".GroupSettings"
            android:label="@string/group_settings" />
        <activity android:name=".EditGroup" />
        <activity
            android:name=".GroupScreen"
            android:label="@string/groupscreen" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homescreen" />
        <activity
            android:name=".ToDoList"
            android:label="@string/to_do_list" />
        <activity
            android:name=".Messenger"
            android:label="@string/messenger" />
        <activity
            android:name=".Calendar"
            android:label="@string/calendar" />
    </application>

</manifest>